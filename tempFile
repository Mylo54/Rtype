
// Function itself
template <typename ComponentA, typename ComponentB>
std::vector<int> getEntities() {
    std::vector<int> res;
    // auto &listA = getComponents<ComponentA>;
    // auto &listB = getComponents<ComponentB
    // for (int i = 0; i < listA.size() && i < listB.size(); i++)
    // {
    //     if (listA[i].has_value() && listB[i].has_value())
    //         res.push_back(i);
            std::cout << "Entity found" << std::endl;
    // }
    return res;
}

// Commande in main to test it
std::vector<int> matches = reg.getEntities<eng::Position, eng::Velocity>();
